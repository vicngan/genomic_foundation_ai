# Frontend: Backend API URL (default: http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000

# Backend: vLLM Server Configuration
# Set these as environment variables when running the backend server
# Example: export VLLM_SERVER_URL="http://your-lighthouse-server-ip:8000/v1"
# Or create a .env file in the backend directory with:
# VLLM_SERVER_URL=http://your-lighthouse-server-ip:8000/v1
# VLLM_API_KEY=not-needed
#
# Note: Replace "your-lighthouse-server-ip" with your actual server IP address
# Common vLLM default ports: 8000, 8001, 8002, etc.
# The /v1 suffix is required for OpenAI-compatible API
